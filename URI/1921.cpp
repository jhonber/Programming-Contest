#include<bits/stdc++.h>
using namespace std;

#define D(x) cout << #x << " = " << (x) << endl;

int main() {
  long long n;
  while (cin >> n) {
    cout << ((n - 3) * n) / 2 << endl;
  }

  return 0;
}
