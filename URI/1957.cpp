#include<bits/stdc++.h>
using namespace std;

#define d(x) cout << #x << " = " << (x) << endl;

int main() {
  int n;
  while (cin >> n) {
    cout << hex << uppercase << n << endl;
  }

  return 0;
}