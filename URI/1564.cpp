#include<bits/stdc++.h>
using namespace std;

#define D(x) cout << #x << " = " << (x) << endl;

int main() {
  int n;
  while (cin >> n) {
    if (n > 0) cout << "vai ter duas!" << endl;
    else cout << "vai ter copa!" << endl;
  }

  return 0;
}